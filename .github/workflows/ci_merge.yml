name: Merge

on:
  push:
    branches:
      - main


jobs:

  merge:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Echo Merge
        run: |
          echo "Merge"