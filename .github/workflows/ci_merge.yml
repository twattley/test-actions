name: CI MERGE

on:
  - pull_request

jobs:

  build-push:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3


      - name: Echo Merge
        run: |
          echo "Merge"